
## Get Started with {framework} on Appz

{intro}

<Note>You can deploy a static **{framework}** site to Appz with zero configuration.</Note>

<p>To get started with **{framework}** on Appz:</p>

-    If you already have a project set up with **{framework}**, first [install](/installation) the Appz CLI. Then, run the <code>appz</code> command from your project's root directory to deploy.
-   Alternatively, clone one of our **{framework}** example repositories to your preferred git provider. You can then deploy it on Appz using the deployment button provided below:
-   Or, select a template from Appz's marketplace:
